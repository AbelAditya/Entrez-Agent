[project]
name = "entrez-agent"
version = "0.1.0"
description = "Agentic workflow to interact with Entrez system using LangGraph"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.4.0",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
    "redis>=8.1.0",
]
